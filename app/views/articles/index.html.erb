<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%# <a href="/articles/<%= article.id %>
      <%# <a href="<%= article_path(article) %>
        <%# <%= article.title %>
      <%# </a> %>
 
      <%# article.title: is the text of the link that will be appear in the html _ article is refrence for the path %>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>
